#include "main.h"

/**
 * main - entry point
 * @argc: the number of argument
 * @argv: the parameter the handle the arguments
 *
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
	if (argv[0])
		printf("%d\n", argc);
	return (0);
}
