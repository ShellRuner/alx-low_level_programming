#include "main.h"

/**
 * main - the entry point
 * @argc: the number of argument
 * @argv: an array that handle argument
 *
 * Return: 0 (success)
 *
 */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
