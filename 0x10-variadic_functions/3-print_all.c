#include "variadic_functions.h"

/**
 * print_all - A function that prints anything
 * @format: the first argument and a list of types of arguments
 *           passed to function
 *
 * Return: Nothing
 */
void print_all(const char * const format, ...)
{
	va_list args;

	va_start(args, format);

}
