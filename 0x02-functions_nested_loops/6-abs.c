#include "main.h"

/**
 * _abs - Entry point
 *
 * @n: THe function argument
 *
 * Return: Always res (Success)
 */
int _abs(int n)
{
	int res;

	if (n < 0)
	{
		res = -n;
	}
	else if (n == 0)
	{
		res = n;
	}
	else
	{
		res = n;
	}

	return (res);
}
